# Dagster workspace configuration
# Defines code locations (repositories) for the deployment

load_from:
  - grpc_server:
      host: dagster-code-location
      port: 4000
      location_name: honey_duck
